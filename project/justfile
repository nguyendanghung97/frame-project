# frame-project — Vite app (`npm run` wrappers).

default:
    @just --list

install:
    npm install

dev:
    npm run dev

build:
    npm run build

preview:
    npm run preview

lint:
    npm run lint
